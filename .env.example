# ============================================
# Marketing Analytics MCP Server Configuration
# ============================================
# Copy this to .env and fill in your values.
# Only configure the APIs you want to use — unconfigured APIs will be skipped.
# Run "npm run setup" for an interactive guide to get these values.

# --- Google OAuth2 (shared by GA4, GSC, Google Ads) ---
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REFRESH_TOKEN=your-refresh-token

# --- GA4 (Google Analytics 4) ---
# Find your property ID: GA4 Admin > Property > Property Details
GA4_PROPERTY_ID=123456789

# --- GSC (Google Search Console) ---
# Your verified site URL. Use "sc-domain:example.com" for domain properties
# or "https://www.example.com/" for URL-prefix properties.
GSC_SITE_URL=https://www.example.com

# --- Meta/Facebook Ads ---
# Long-lived user token or system user token with ads_read permission
META_ACCESS_TOKEN=your-meta-access-token
# Optional: for app secret proof (recommended for security)
META_APP_ID=your-app-id
META_APP_SECRET=your-app-secret

# --- Google Ads ---
# Developer token from Google Ads API Center (Tools > API Center)
GOOGLE_ADS_DEVELOPER_TOKEN=your-developer-token
# Customer account ID (10 digits, no dashes)
GOOGLE_ADS_CUSTOMER_ID=1234567890
# Optional: MCC manager account ID (if accessing client accounts via manager)
# GOOGLE_ADS_LOGIN_CUSTOMER_ID=9876543210
